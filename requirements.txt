# Development dependencies (optional — install only if you run tests or lint)
# No runtime dependencies — the engine uses only the Python standard library.
#
# Install: uv pip install -r requirements.txt
# Or:      pip install -r requirements.txt
#
# Pin to exact versions for reproducible builds:
pytest==8.2.2
pytest-cov==5.0.0
black==24.4.2
flake8==7.1.0
mypy==2.1.0

# Security scanner (optional — run: bandit -r scripts/)
bandit==1.7.9

# Dependency vulnerability checker (optional — run: pip-audit)
pip-audit==2.10.1
